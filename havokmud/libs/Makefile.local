#
# Makefile.local
#

all-recursive:
	[ "${SUBDIRS}" != "" ] && \
	for i in ${SUBDIRS} ; do ${MAKE} -C $$i -f Makefile.gjh all ; done

clean-recursive:
	[ "${SUBDIRS}" != "" ] && \
	for i in ${SUBDIRS} ; do ${MAKE} -C $$i -f Makefile.gjh clean ; done
