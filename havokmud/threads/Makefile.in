##
## The compiler your gonna use to compile this with..
##
CC=@CC@
##
LD=$(CC)
RANLIB=@RANLIB@

ROOT=..

## Include dirs
INCDIRS = -I. -I${ROOT} -I${ROOT}/include -I${ROOT}/util -I/usr/include/mysql

##  these are architecture dependent flags
CFLAGS = -g -O6 -Wall -Werror -Wno-trigraphs -Wno-long-long -std=c99 -pedantic \
	 ${INCDIRS}

##  Libraries
LIBS = @LIBS@
LDFLAGS = @LDFLAGS@


SRCS =	master.c connections.c input.c login.c dns.c logging.c editor.c
SRCS += playing.c mysql_handler.c
ARCHIVE =	libhavokthread.a

OBJS = ${SRCS:.c=.o}
DEPS = ${SRCS:.c=.d}

all : $(ARCHIVE)

$(ARCHIVE) : ${OBJS}
	$(AR) rcv $@ $+
	$(RANLIB) $@

depend : ${DEPS}

${DEPS} : Makefile

%.d: %.c
	@echo "Making $@ (dependencies for $<)"
	@${CC} -MM ${CFLAGS} $< | sed 's,\($*\)\.o[ :]*,\1.o $@ : ,g'  > $@

.PHONY: clean cleanall

clean:
	rm -f $(OBJS) $(ARCHIVE)

cleanall: clean
	rm -f ${DEPS}

distclean: cleanall
	rm -f Makefile

Makefile: Makefile.in ${ROOT}/configure
	@echo Your Makefile is out of date, rerun ${ROOT}/configure
	

-include ${DEPS}

